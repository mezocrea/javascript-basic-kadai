// 関数の定義（アロー関数）
const square = (num) => {
  return num * num;
}

// 戻り値の出力
console.log(square(100));


// 関数の定義（関数宣言）
function funcSquare(number) {
  return number * number;
}

// 戻り値の出力
console.log(funcSquare(10));